#include <bits/stdc++.h>
using namespace std;
int main()
{
    string s = "Hi, Nur";
    string p = ", Hello";
    // s += p;
    // s.append(p);
    // s.push_back('O');
    // s.pop_back();
    // s = "Bye";
    // s = p;
    // s.assign(p);
    // s.erase(2,1);
    // s.replace(4, 3, "Sohag");
    s.insert(7, "e Alam");
    cout << s << endl;
    return 0;
}